# import copy
# from collections import deque

# n, k = map(int,input().split())

# a = deque(list(input()))

# while k != 0:
#     maxnum = 0
#     maxlist = deque([])
#     for i in k:
#         if a[0] > maxnum :
#             max_List.clear()
#             maxnum = max_list.append(a.popleft)